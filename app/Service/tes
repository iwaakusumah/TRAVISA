<div class="row">
    <div class="card col-12">
        <div class="card-body">
            @if(session('error'))
            <div class="alert alert-danger">
                {{ session('error') }}
            </div>
            @endif
            <form action="{{ route('staff-student.calculation.promethee') }}" method="POST">
                @csrf
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label for="period_id">Periode:</label>
                        <select id="period_id" name="gender" class="form-select" required>
                            <option value="" disabled selected>-- Pilih Periode --</option>
                            @foreach($periods as $period)
                            <option value="{{ $period->id }}" {{ old('period_id') == $period->id ? 'selected' : '' }}>
                                {{ $period->name }}
                            </option>
                            @endforeach
                        </select>
                    </div>
                </div>
        </div>
        </form>
    </div>
</div>